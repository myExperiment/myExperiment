<h1>Editing relationship</h1>

<%= error_messages_for :relationship %>

<% form_for(:relationship, :url => relationship_path(@relationship.network_id, @relationship), :html => { :method => :put }) do |f| %>
  <p>
    <%= submit_tag "Update" %>
  </p>
<% end %>

<%= link_to 'Show', relationship_path(params[:network_id], @relationship) %> |
<%= link_to 'Back', relationships_path(params[:network_id]) %>