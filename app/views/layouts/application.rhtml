<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
       "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">

<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
<head>
  <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
  <title>myExperiment.org - M2 - <%= controller.action_name %></title>
  <%= stylesheet_link_tag 'scaffold' %>
</head>
<body>

<p style="color: green"><%= flash[:notice] %></p>

<% if logged_in? %>
<p>
  Logged in as <%= profile_link(current_user.id) %> [<%= link_to "Logout", url_for(:controller => 'auth', :action => 'logout') %>] - 
  <%= messages_link(current_user.id) %> - 
  <%= memberships_pending_link(current_user.id) %> - 
  <%= friendships_pending_link(current_user.id) %>
</p>
<% else %>
[<%= link_to "Login", url_for(:controller => 'auth', :action => 'login') %>]
<% end %>

<p>
  <%= link_to "Networks", networks_path %> <% if logged_in? %>(<%= link_to "Mine", owned_networks_path(current_user.id) %>)<% end %> 
  | <%= link_to "Users", users_path %> <% if logged_in? %>(<%= link_to "Me", user_path(current_user.id) %>)<% end %> 
  | <%= link_to "Profiles", profiles_path %> <% if logged_in? %>(<%= link_to "Me", profile_path(current_user.profile.id) %>)<% end %> 
  | <%= link_to "Pictures", url_for(:controller => 'pictures') %> <% if logged_in? %>(<%= link_to "Mine", pictures_path(current_user.id) %>)<% end %> 
  <% if logged_in? %>| <%= link_to "Policies", policies_path %><% end %>
</p>

<hr/>

<%= yield  %>

</body>
</html>
