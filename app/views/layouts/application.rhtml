<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
       "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">

<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
<head>
  <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
  <title>myExperiment.org - M2 - <%= controller.controller_name %> - <%= controller.action_name %></title>
  <%= stylesheet_link_tag 'scaffold' %>
  <%= stylesheet_link_tag 'savagebeast' %>
  <%= stylesheet_link_tag 'acts_as_taggable_stylesheet' %>
  <%= stylesheet_link_tag 'star_rating' %>
  <%= javascript_include_tag :defaults %>
</head>
<body>

<%= image_tag("logo.png") %>

<p style="color: red"><%= flash[:error] %></p>
<p style="color: green"><%= flash[:notice] %></p>

<% if logged_in? %>
<p>
  Logged in as <%= profile_link(current_user.id) %> [<%= link_to "Logout", session_path, :method => :delete %>] - 
  <%= messages_link(current_user.id) %> - 
  <%= memberships_pending_link(current_user.id) %> - 
  <%= friendships_pending_link(current_user.id) %> - 
  <%= link_to "My Policies", policies_path %> - 
  <%= link_to "My Bookmarks", bookmarks_path %>
</p>
<% else %>
  <% form_tag session_path do %>
    <label for="username">Username:</label> <%= text_field_tag 'session[username]' %> 
    <label for="password">Password:</label> <%= password_field_tag 'session[password]' %> 
    <label for="remember_me">Remember me:</label> <%= check_box_tag 'session[remember_me]' %> ...or use: 
    <label for="openid_url">OpenID:</label> <%= text_field_tag 'openid_url' %> 
    <%= submit_tag 'Sign in', :disable_with => "Signing in&hellip;" %>
  <% end %>
<% end %>

<p>
  <%= link_to "Networks", networks_path %> <% if logged_in? %>(<%= link_to "Mine", owned_networks_path(current_user.id) %>)<% end %> 
  | <%= link_to "Users", users_path %> <% if logged_in? %>(<%= link_to "Me", user_path(current_user.id) %>)<% end %> 
  | <%= link_to "Contributions", contributions_path %> <% if logged_in? %>(<%= link_to "Mine", url_for(:controller => 'contributions', :contributor_id => current_user.id, :contributor_type => current_user.class.to_s) %>)<% end %> 
</p>
    
<p>
  <% form_tag search_workflows_path, :method => :get do %>
    <fieldset><legend>Search</legend>
    <%= text_field_tag :query %> <%= submit_tag "Find Workflows" %>
    </fieldset>
  <% end %>
</p>

<hr/>
    
<%= yield  %>

<hr/>

<p>
  <%= tag_cloud %>
</p>

<p>
  <b>What is a <%= link_to 'tag cloud', "http://en.wikipedia.org/wiki/Tag_cloud" %>?</b>
</p>

</body>
</html>
