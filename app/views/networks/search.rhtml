<% t "Search" -%>

<h1>Search Results</h1>

<h2>Showing <%= pluralize(@networks.length, "group") %> of <%= @networks_found_total_count -%> found for <%= @query ? "\"#{@query}\"" : "" %></h2>	

<%= render :partial => "networks/table", :locals => { :collection => @networks } %>

<br />
