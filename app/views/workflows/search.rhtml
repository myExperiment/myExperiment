<h3>Search Results</h3>

<h1><%= pluralize(@workflows.length, "Workflow") %> found</h1>

<p>
  <b>by any <%= params["contributor_type"] || "contributor" %> <%= params["contributor_id"] ? "with ID #{params["contributor_id"]}" : "" %> <%= @query ? "using the query: \"#{@query}\"" : "" %></b>
</p>

<%= render :partial => "workflows/table", :locals => { :collection => @workflows } %>

<br />
