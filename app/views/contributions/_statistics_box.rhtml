<div class="contribution_section_box">
	<p class="heading" style="margin-bottom: 0.3em;">
		Statistics
	</p>
	
	<div class="stats_box"> 
	
		<% type = contributable.class.to_s -%>
		
		<%= render :partial => "contributions/statistics", :object => contributable.contribution, :locals => { :type => type } %>
		
		<% if allow_credits_and_attributions? type -%>
			<p>
				<%= pluralize contributable.creditors.length, "credit" %> 
			</p>
			
			<p>
				<%= pluralize contributable.attributors.length, "attribution" %> 
			</p>
		<% end -%>
		
		<% if taggable? type -%>
			<p>
	      <%= pluralize contributable.taggings.length, "tag" %>
	    </p>
		<% end -%>
		
		<% if rateable? type -%>
			<p>
	      <%= pluralize contributable.ratings.length, "rating" %>
	    </p>
		<% end -%>
		
		<% if commentable? type -%>
	    <p>
	    	<%= pluralize contributable.comments.length, "comment" %>
	    </p>
		<% end -%>
		
		<% if reviewable? type %>
			<p>
	      <%= pluralize contributable.reviews.length, "review" %>
	    </p>
		<% end %>
	
	</div>
</div>