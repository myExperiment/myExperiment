<h1>Search Results</h1>

<h2><%= pluralize(@blobs.length, "Files") %> found for <%= @query ? "\"#{@query}\"" : "" %></h2>

<%= render :partial => "blobs/table", :locals => { :collection => @blobs } %>

<br />
