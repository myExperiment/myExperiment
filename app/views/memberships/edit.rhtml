<h1>Editing membership</h1>

<%= error_messages_for :membership %>

<% form_for(:membership, :url => membership_path(@membership.network_id, @membership), :html => { :method => :put }) do |f| %>
  <p>
    <%= submit_tag "Update" %>
  </p>
<% end %>

<%= link_to 'Show', membership_path(@membership.network_id, @membership) %> |
<%= link_to 'Back', memberships_path %>