<h1>Listing friendships</h1>

<p>The <strong>User</strong> initiates the Friendship request with the <strong>Friend</strong> who can accept/reject.</p>

<table>
  <tr>
    <th>User</th>
    <th>Friend</th>
    <th>Created at</th>
    <th>Accepted at</th>
  </tr>
  
<% for friendship in @friendships %>
  <tr>
    <td><%=h friendship.user_id %></td>
    <td><%=h friendship.friend_id %></td>
    <td><%=h friendship.created_at %></td>
    <td><%=h friendship.accepted_at %></td>
    <td><%= link_to 'Show', friendship_path(friendship.friend_id, friendship) %></td>
    <td><%= link_to 'Edit', edit_friendship_path(friendship.friend_id, friendship) %></td>
    <td><%= link_to 'Destroy', friendship_path(friendship.friend_id, friendship), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New friendship', new_friendship_path %>